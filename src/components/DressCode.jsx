export default function PresenceMessage() {
  return (
    <section className="section-shell" style={{ paddingTop: 'clamp(2rem, 5vw, 3rem)', paddingBottom: 'clamp(2rem, 5vw, 3rem)' }}>
      <div className="section-title" style={{ marginBottom: '1.25rem' }}>Your Presence, Our Greatest Gift</div>
      <div className="card" style={{ padding: 'clamp(1.4rem, 3vw, 2rem)', textAlign: 'center' }}>
        <p style={{ margin: 0, maxWidth: '32rem', marginLeft: 'auto', marginRight: 'auto', fontSize: 'clamp(1rem, 1.2vw, 1.1rem)', lineHeight: 1.8 }}>
          More than anything, we ask for your blessings and your presence
          on our special day. Having you there to celebrate with us
          means more than any gift ever could.
        </p>
      </div>
    </section>
  );
}
